# Copy this file to secrets.toml (which is gitignored) for local runs, OR paste
# these contents into your host's Secrets box (Streamlit Community Cloud:
# App → Settings → Secrets). NEVER commit the real key.

ANTHROPIC_API_KEY = "sk-ant-your-key-here"
